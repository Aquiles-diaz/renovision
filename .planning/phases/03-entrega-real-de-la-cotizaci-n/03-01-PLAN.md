---
phase: 03-entrega-real-de-la-cotizaci-n
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Studio3D.jsx
  - package.json
autonomous: true
requirements: [QUOTE-01, QUOTE-03]
user_setup: []

must_haves:
  decisions: [D-01, D-02, D-03, D-08]  # CONTEXT.md decisions delivered by this plan (PDF generation + honest download labels)
  truths:
    - "Clicking 'Descargar ficha técnica (PDF)' downloads a real .pdf file (not .html)"
    - "Clicking 'Descargar presupuesto (PDF)' downloads a real .pdf file (not .html)"
    - "The generated PDF reuses the existing document HTML/CSS layout (logo, specs, total)"
    - "A failure during PDF generation does not freeze the button or crash the Studio"
    - "html2pdf.js is not in the initial bundle (loaded only when a download is clicked)"
  artifacts:
    - path: "src/Studio3D.jsx"
      provides: "downloadDoc(kind) rewritten to emit real PDF via dynamic html2pdf import"
      contains: "html2pdf"
    - path: "package.json"
      provides: "html2pdf.js dependency declared"
      contains: "html2pdf.js"
  key_links:
    - from: "src/Studio3D.jsx downloadDoc"
      to: "html2pdf.js"
      via: "dynamic import inside the handler"
      pattern: "await import\\([\"']html2pdf"
    - from: "cotizar__actions download buttons"
      to: "downloadDoc"
      via: "onClick handlers with honest labels"
      pattern: "downloadDoc\\(\"(tecnico|presupuesto)\"\\)"
---

<objective>
Convert the two document-download buttons from a misleading `.html`-disguised-as-PDF download into a real client-side PDF, and make their labels honest. Closes QUOTE-01 and the download-button half of QUOTE-03.

Purpose: A quoting tool must deliver a real PDF when it says "PDF". The current `downloadDoc` builds a `text/html` Blob and downloads a `.html` file (`src/Studio3D.jsx:542-550`) — a trust gap flagged in CONCERNS.md (Medium).
Output: Rewritten `downloadDoc(kind)` using `html2pdf.js` (dynamic import per D-03), `.pdf` filenames (D-02), honest button copy (D-08), and `html2pdf.js` added to package.json.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-entrega-real-de-la-cotizaci-n/03-CONTEXT.md
@.planning/phases/03-entrega-real-de-la-cotizaci-n/03-PATTERNS.md
@.planning/phases/03-entrega-real-de-la-cotizaci-n/03-UI-SPEC.md

<interfaces>
<!-- Current downloadDoc and the action bar. Extracted from src/Studio3D.jsx — use directly. -->

downloadDoc(kind) lives at src/Studio3D.jsx:498-551. It builds an HTML string (`html`, lines 508-541) from in-scope values: `furniture`, `room`, `width`, `quote` (with `quote.breakdown`, `quote.bays`, `quote.total`), `mat`, `leg`, `line`, `cfg`, and `fmtAR`. CONTEXT D-01 mandates KEEPING this HTML/CSS string verbatim; only the output tail changes.

Current download tail to REPLACE (src/Studio3D.jsx:542-550):
  new Blob([html], { type: "text/html" }) → createObjectURL → <a download="...html"> → click → revokeObjectURL after 4000ms

Action bar at src/Studio3D.jsx:706-716 (the two `.dlbtn` download buttons are the scope of this plan; the `.btn--red` "Solicitar →" CTA at 713-715 is OUT of scope here — Plan 02 rewires it):
  <button className="dlbtn" onClick={() => downloadDoc("tecnico")}>↓ PDF Técnico</button>
  <button className="dlbtn" onClick={() => downloadDoc("presupuesto")}>↓ Presupuesto</button>

Precedent for dynamic-import + cleanup hygiene — openAR (src/Studio3D.jsx:472-483): always reset the loading flag in `.catch`; never leave an object URL dangling (closeAR revokes).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add html2pdf.js dependency</name>
  <files>package.json</files>
  <read_first>
    - package.json (current deps: react, react-dom, three, @google/model-viewer — no html2pdf yet)
    - .planning/phases/03-entrega-real-de-la-cotizaci-n/03-PATTERNS.md (Metadata: new npm deps this phase)
  </read_first>
  <action>Install `html2pdf.js` as a runtime dependency with `npm install html2pdf.js`. This is the package named in CONTEXT D-01/D-03 and PATTERNS.md. It will be dynamic-imported (D-03) so it stays out of the initial bundle, but it must still be declared in package.json `dependencies`. Do not pin to a manual version string by hand — let npm resolve and write the caret range into package.json and update package-lock.json.</action>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(!p.dependencies['html2pdf.js']) process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - package.json `dependencies` contains a `html2pdf.js` entry
    - package-lock.json updated (npm install ran, not a hand-edit)
    - `npm run build` exits 0 (no resolution errors)
  </acceptance_criteria>
  <done>html2pdf.js is a declared dependency and the project still builds.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite downloadDoc to emit a real PDF</name>
  <files>src/Studio3D.jsx</files>
  <read_first>
    - src/Studio3D.jsx:498-551 (the downloadDoc function — the exact code being rewritten)
    - src/Studio3D.jsx:472-489 (openAR/closeAR — the dynamic-import + loading-flag + revoke hygiene to mirror)
    - .planning/phases/03-entrega-real-de-la-cotizaci-n/03-CONTEXT.md (D-01, D-02, D-03)
    - .planning/phases/03-entrega-real-de-la-cotizaci-n/03-PATTERNS.md (downloadDoc rewrite section — the replacement shape)
  </read_first>
  <action>Make `downloadDoc` an `async` function. KEEP the entire `html` string construction (src/Studio3D.jsx:508-541) verbatim per D-01 — do not change the document layout or CSS. REPLACE only the download tail (lines 542-550). New tail: dynamic-import the library with `const { default: html2pdf } = await import("html2pdf.js")` (D-03 — keeps it out of the initial bundle); build a detached container element, set its `innerHTML` to the SAME `html` string; call `html2pdf().set({ filename: ... }).from(container).save()` and await it. The filename must use the `.pdf` extension (D-02): `reno-${furniture.id}-${tech ? "ficha-tecnica" : "presupuesto"}.pdf`. Wrap the import+generate in `try/catch`; on failure log via `console.error` and do not throw past the handler (mirror openAR's `.catch` discipline so a PDF failure never freezes the UI). Remove the old `Blob`/`createObjectURL`/`<a download>`/`revokeObjectURL(...4000)` code entirely so no `text/html` blob or dangling object URL remains.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Source: src/Studio3D.jsx no longer contains `new Blob([html]` or `a.download` with a `.html` filename
    - Source: src/Studio3D.jsx contains `await import("html2pdf.js")` (or single-quote equivalent) inside downloadDoc
    - Source: the download filename string ends in `.pdf` (grep for `presupuesto}.pdf` / `ficha-tecnica`)
    - Source: the html string (lines ~508-541, logo/specs/total/foot) is unchanged
    - Source: downloadDoc is declared `async` and wraps generation in try/catch with a console.error on failure
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>downloadDoc generates a real PDF via lazily-loaded html2pdf, reuses the existing markup, names the file .pdf, and degrades gracefully on error.</done>
</task>

<task type="auto">
  <name>Task 3: Honest labels on the two download buttons</name>
  <files>src/Studio3D.jsx</files>
  <read_first>
    - src/Studio3D.jsx:706-716 (the cotizar__actions bar — the two .dlbtn buttons)
    - .planning/phases/03-entrega-real-de-la-cotizaci-n/03-UI-SPEC.md (Copywriting Contract → Action bar)
    - .planning/phases/03-entrega-real-de-la-cotizaci-n/03-CONTEXT.md (D-08)
  </read_first>
  <action>Update the copy of the two `.dlbtn` buttons to the prescribed honest labels from UI-SPEC (D-08), since the documents are now real PDFs. The `tecnico` button label becomes `↓ Descargar ficha técnica (PDF)` and the `presupuesto` button label becomes `↓ Descargar presupuesto (PDF)`. Keep the existing `dlbtn` class and the `onClick={() => downloadDoc("tecnico")}` / `onClick={() => downloadDoc("presupuesto")}` wiring unchanged. Do NOT touch the `.btn--red` "Solicitar →" CTA at line 713-715 — that is Plan 02's responsibility.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Source: src/Studio3D.jsx contains the strings `Descargar ficha técnica (PDF)` and `Descargar presupuesto (PDF)`
    - Source: the old labels `PDF Técnico` and the bare `Presupuesto` dlbtn label are gone
    - Source: the `.btn--red` "Solicitar →" button is unchanged (still present, untouched)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>The two download buttons honestly describe that they download PDFs.</done>
</task>

</tasks>

<verification>
- `npm run build` exits 0 after all tasks.
- Manual smoke (executor self-check, not a gate): clicking either download button produces a `.pdf` download with the same visual layout as the prior HTML document.
- No `text/html` Blob or `.html` filename remains anywhere in downloadDoc.
- html2pdf.js appears only via dynamic import (not a top-level import), keeping it out of the initial chunk.
</verification>

<success_criteria>
QUOTE-01 satisfied: both document buttons generate a real client-side PDF (no .html disguised as PDF). The download-button portion of QUOTE-03 satisfied: labels honestly say "Descargar … (PDF)". The 3D/AR experience is untouched (only downloadDoc and two button labels changed).
</success_criteria>

<output>
Create `.planning/phases/03-entrega-real-de-la-cotizaci-n/03-01-SUMMARY.md` when done.
</output>
